const nextConfig = {
  output: 'export',
  // If you use next/image, you may need to disable image optimization:
  // images: { unoptimized: true },
  // Set basePath to your repository name if hosting under a subdirectory:
  // basePath: '/your-repo-name',
};

module.exports = nextConfig;   
